---
- set_fact:
    user: bstineha

- name: Get info for a single service
  win_service_info:
    name: W3SVC
  register: service_info

- name: Show webserver details
  debug: 
    msg: "{{ ansible_host }}"


